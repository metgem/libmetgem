#ifndef ARGPARTITION_H
#define ARGPARTITION_H

#include <algorithm>
#include <numeric>
#include <vector>

// template <typename T>
std::vector<int> argpartition(std::vector<float> const &vec, const int &kth);
#endif /* ARGPARTITION_H */clas